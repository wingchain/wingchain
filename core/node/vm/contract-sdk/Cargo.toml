[package]
name = "node-vm-contract-sdk"
version = "0.1.0"
authors = ["guoxbin <guoxbin@gmail.com>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
contract-sdk-macro = { package = "node-vm-contract-sdk-macro", path = "../contract-sdk-macro" }
contract-sdk-primitives = { package = "node-vm-contract-sdk-primitives", path = "../contract-sdk-primitives" }
